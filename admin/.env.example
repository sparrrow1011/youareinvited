# Django backend URL used by rewrites and login proxy (no trailing slash)
# Prefer BACKEND_URL. NEXT_PUBLIC_BACKEND_URL is supported as a fallback for older deployments.
BACKEND_URL=https://your-backend-project.vercel.app
NEXT_PUBLIC_BACKEND_URL=https://your-backend-project.vercel.app
