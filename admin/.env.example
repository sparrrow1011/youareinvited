# Production Django backend used by rewrites and the login proxy (no trailing slash).
# Set BACKEND_URL=http://127.0.0.1:8000 explicitly only when developing
# against a local Django server. NEXT_PUBLIC_BACKEND_URL is a legacy fallback.
BACKEND_URL=https://backend.v0.youare-invited.com
NEXT_PUBLIC_BACKEND_URL=https://backend.v0.youare-invited.com
